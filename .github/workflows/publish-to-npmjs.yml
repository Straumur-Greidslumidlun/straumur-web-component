# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Publish Package to npmjs

on:
  release:
    types: [created]

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - run: npm ci

      # dist/ is not committed; the artifacts are always built fresh here.
      # npm publish additionally runs prepublishOnly (typecheck + test + build) as a safety net.
      - name: Build
        run: npm run build

      - name: Publishing pre-release
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          npm publish --tag next --provenance --access public

      - name: Publishing stable release
        if: ${{ github.event_name == 'release' }}
        run: |
          npm publish --provenance --access public
